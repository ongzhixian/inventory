@{
    ViewData["Title"] = "Home Page";
    //Layout = "_Login_Layout";
}

<div class="pure-g">

    <div class="pure-u-1-3"></div>
    
    <div class="pure-u-1-3">

        <h1 style="text-align:center">Inventory</h1>

        @if (User.Identity.IsAuthenticated)
        {
            <p>
                <span style="font-weight:bold;font-family:'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif">
                    TODO:
                </span>
                Some real content.
            </p>

        }
        else
        {
        <div>
            <p style="border:1px solid #ccc; background-color: #333; color: #ccc; padding:1em; border-radius:1em;">
                You are currently unidentified to the system. <br/>
                @Html.ActionLink("Login", "Login", "Account") now to identify yourself to the system.
            </p>
        </div>
        }


        <partial name="_FooterPartial" />
    </div>

    <div class="pure-u-1-3"></div>
</div>